"""Tests for the reward function.

Covers: closing in on the target scores higher than drifting away, thrusting is
never free, the terms in the returned breakdown sum to the reward, and the
terminal bonus and penalty carry the expected signs.
"""

import pytest

pytest.skip("rewards.py is still a skeleton", allow_module_level=True)
